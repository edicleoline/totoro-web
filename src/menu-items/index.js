import dashboard from './dashboard';
import project from './project';
import configurations from './configurations';
import support from './support';

const menuItems = {
    items: [dashboard, project, configurations, support]
};

export default menuItems;
